'use client';

import { useState, useEffect, useRef } from 'react';
import { useTheme } from '@/contexts/ThemeContext';

/**
 * ThemeToggle component that provides a dropdown to switch between light, dark, and system themes
 */
export default function ThemeToggle() {
  // Hide theme toggle for now since we only have dark mode
  // TODO: Re-enable when light mode is properly implemented
  return null;
}
